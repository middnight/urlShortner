package com.url.shortner.url_shortner.service.validator;

public class ValidatorServiceImpl implements ValidatorService {
  @Override
  public void validateUrl(String url) {
    // java does not have a standard way to validate url :(
    return;
  }
}
